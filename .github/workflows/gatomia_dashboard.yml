--- a/.github/workflows/gatomia_dashboard.yml
+++ b/.github/workflows/gatomia_dashboard.yml
@@ -38,15 +38,18 @@
     jobs:
       build-dashboard:
-        runs-on: ubuntu-latest
-        env:
-          NEO4J_URI: ${{ secrets.NEO4J_URI }}
-        # outras vars
-        steps:
-          - name: Checkout
-            uses: actions/checkout@v3
-          - name: Do something
-            run: echo "..."
-            env:
-              NEO4J_USER: ${{ secrets.NEO4J_USER }}
-          - run: echo "Extra run duplicado"   # erro aqui provavelmente
+        runs-on: ubuntu-latest
+        env:
+          NEO4J_URI: ${{ secrets.NEO4J_URI }}
+          NEO4J_USER: ${{ secrets.NEO4J_USER }}
+          NEO4J_PASSWORD: ${{ secrets.NEO4J_PASSWORD }}
+          COMMIT_ID: ${{ github.sha }}
+          COMMIT_MSG: ${{ github.event.head_commit.message }}
+          AUTHOR: ${{ github.event.pusher.name }}
+        steps:
+          - name: Checkout repository
+            uses: actions/checkout@v3
+          
+          - name: Run dashboard script
+            run: |
+              echo "Iniciando Dashboard..."
+              # comandos do dashboard aqui
+          
+          # outros steps separados, cada com seu `name:` e `run:` ou `uses:`
