name: Menir — Triggers & Checkpoint

on:
  push:
    branches: [ "main" ]
    paths:
      - "setup_triggers.cypher"
      - "checkpoint_queries.cypher"

jobs:
  sync-triggers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Setup Triggers
        run: |
          cypher-shell -a $NEO4J_URI -u $NEO4J_USER -p $NEO4J_PASSWORD -f setup_triggers.cypher
