---
description: Universal Laws of the Menir Cortex
---

# Menir Core Universal Directives

1. **Zero-Bloat**: I/O estritamente assíncrono.
2. **Multi-Tenancy**: Isolamento absoluto entre Tenant:BECO e Tenant:SANTOS via JWT.
3. **Lossless Export**: É proibido omitir ou truncar código nas entregas.

## 🧱 MENIR V5.2 ARCHITECTURAL TOPOLOGY (CHECKPOINT)

### 🔴 TIER 1: KERNEL & SYNAPSE (BACKEND) - [LOCKED]
* **State:** Zero-Entropy / Phase 43 Part 2 COMPLETED / CI-CD Exit Code 0.
* **OGM Bridge:** Pydantic V2 `strict=True` + `ConfigDict(extra="allow")` mapeado para o Neo4j. *Zombie Properties* mitigadas.
* **Galvanic Isolation:** Inversão de Controle assíncrona operante via `contextvars.ContextVar[str | None]`. Vazamento entre Tenants (`BECO` e `SANTOS`) fisicamente bloqueado.
* **Concurrency Guard:** `asyncio.Semaphore` e TTL de 30s protegendo o Watchdog contra Deadlocks.
* **OpenAPI Contract:** Especificação 3.1.0 gerada dinamicamente (`menir_v3_spec.json`) atuando como Fonte da Verdade.

### 🔵 TIER 2: ENTERPRISE DASHBOARD (FRONTEND) - [VERIFIED]
* **State:** Auth Bridge Established / SPA Render-Blocking Active.
* **Stack:** Vite + React + TypeScript (Zero-Bloat, sem Axios, sem Tailwind).
* **Contract-Driven Client:** AST Transpilation via `openapi-typescript` (`schema.d.ts`). Interfaces estritas geradas a partir da Tier 1.
* **Transport Layer:** Wrapper `openapi-fetch` com interceptor de requisição para injeção global de JWT (Galvanic Header) e interceptor de resposta para expurgo de sessão (HTTP 401/403).
* **Boundary Security:** HOC `<AuthGuard />` validando o token físico no `localStorage` antes da montagem da árvore de componentes privados. Componente `<Login />` executando handshake I/O real.

**Next Sequence (Pending):**
1. Resolve Remote State Drift (`git pull --rebase`).
2. Execute Phase 44: Enterprise Dashboard & Swiss-Clinical CSS Modular Shell.